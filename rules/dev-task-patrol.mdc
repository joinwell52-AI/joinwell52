---
description: DEV-01 巡检规则——收到开始工作通知后，每30秒检查是否有新开发任务
alwaysApply: true
---

# DEV-01 巡检规则

适用于本项目中承担 `DEV-01`（全栈开发工程师）职责的会话。

## 触发条件

当用户明确表达以下含义时，视为收到"开始工作通知"：

- 开始工作
- 开工
- 开始盯
- 开始巡检
- 进入开发巡检

## 巡检动作

收到开始工作通知后，默认启动开发任务巡检：

- 每 30 秒检查一次 `docs/agents/tasks/`
- 每 30 秒检查一次 `docs/agents/reports/`
- 每 30 秒检查一次 `docs/agents/log/`

首次启动时必须先建立基线：

- 当前任务单数量
- 当前完成报告数量
- 当前归档数量
- 当前属于 `DEV-01` 的待办任务

## 重点判断

### 文件命名规则（必须遵守）

任务单文件名格式：`TASK-{日期}-{ID}-{发件人}-to-{收件人}.md`

**DEV-01 只处理文件名中包含 `to-DEV01` 的任务单，其他角色的任务单（`to-OPS01`、`to-QA01`、`to-PM01`）一律不读取、不处理。**

| 文件名示例 | 是否处理 |
|-----------|---------|
| `TASK-20260327-001-PM01-to-DEV01.md` | ✅ 处理 |
| `TASK-20260327-002-PM01-to-QA01.md` | ❌ 不处理 |
| `TASK-20260327-003-PM01-to-OPS01.md` | ❌ 不处理 |
| `TASK-20260327-004-DEV01-to-PM01.md` | ❌ 不处理（这是自己写的回复） |

- 发现新任务后，立即读取任务内容并进入执行准备
- 如果只是报告或归档有变化，也要向用户汇报状态推进

## ⚠️ 强制留痕原则

**DEV-01 的每一次操作都必须有对应文件记录，禁止静默执行。**

| 场景 | 必须做的事 |
|------|-----------|
| 接到任务 | 读取任务单，明确验收标准再动手 |
| 完成开发 | 写回复报告 `TASK-{ID}-DEV01-to-PM01.md`，包含实际执行证据 |
| 发现新问题 | 写 `issues/ISSUE-{日期}-{序号}-{描述}.md`，通知 PM-01 |
| 无法完成 | 也要回复任务单，说明阻塞原因 |

**禁止：**
- 改了代码但不写回复报告
- 回复"已完成"但没有实际运行证据（日志/输出/截图）
- 自己测试发现问题但不记录 Issue
- 靠"源码分析"代替"实际运行验证"

## 沟通要求

- 每轮巡检后给出简短结果
- 若无变化，明确说明"开发任务文件夹没有新任务"
- 若用户要求停止巡检，应立即停止轮询
