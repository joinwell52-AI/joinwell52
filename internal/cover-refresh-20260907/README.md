# 六篇题图更新 · 2026-09-07

用户指定六篇文章更换题图。已按反馈采用钴蓝主色、明亮环境、规整三维表达；不把规整误作必须写实。只修改封面引用及对应图片，正文保持原样。

## 冻结与版本

- Weekly 008 的方正双端连接器得到用户“可以！”确认，原始像素保存为 `weekly-008-user-approved-original.png`，不再变动。WebP 为网页交付压缩版。
- 早期暗色、不规则陶瓷雕塑、实景楼梯均不采用；未发布到网站。
- 风险图中的自动生成英文标签已经移除，随后按用户要求把主体改成钴蓝。保存的版本不含文字。
- 其余四张采用恢复路径、受限文件夹、尚未落下的批准印记、受压计算模块，分别表达各篇判断。
- 具体创作包与正向提示词保存在逐篇 JSON；`delivery-manifest.json` 绑定最终源图、文章和复核结论，`verification.json` 保存 SHA-256 与检查结果。
- 生成使用内置 image_gen，每篇单独生成；风险图的局部编辑提示词保存在其创作包。最终四张概念图均为各自新创作包的第一次生成。

## 交付范围与验证

- 12 个中英文文章文件只更换两个封面引用，正文逐字比对保持不变。
- 5 份 Daily 的候选 PNG 与历史公开 PNG 路径同步为同一新像素，避免再发布从旧候选恢复旧图。旧版本仍可通过更新前 Git 历史恢复。
- 文章页使用新的带版本文件名 WebP；构建生成 336×189 列表缩略图，避免列表下载原始大 PNG。
- 本地完整 `npm run docs:build` 通过，严格 SSR 检查及 Runtime Projection 检查通过。
- `npm run publication:layout:validate` 通过：398 个文章文件、98 个正文图片、27 个候选批次。
- `verify-covers.mjs` 确认 6 对文章、12 个生成页面、6 张列表缩略图、图片尺寸与原始像素一致性。
- 构建产生的无关投影不纳入本次封面提交。
