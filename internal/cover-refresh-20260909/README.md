# Six article cover replacements — 2026-09-09

Replaced only the six September 7–8 covers identified in the user's screenshots.
The full Chinese articles were read before each article-specific brief. Built-in image generation was used once per cover. Art direction follows the user's cobalt-blue, bright, orderly conceptual-editorial preference.

## Scope and preservation

- Twelve published Markdown pages: only frontmatter cover and ArticleCover image URLs change.
- Six new versioned 1600 × 900 WebP assets; historical source images and production evidence remain unchanged.
- Website thumbnails are regenerated by the existing build at 336 × 189.
- The user's dirty D:\TMPA\joinwell52 checkout was not edited; work is in an isolated worktree based on 685f9c90.
- Historical staging packets retain their original covers; this is a post-publication visual revision.

## Evidence

[Manifest](manifest.json) records asset paths, hashes, exact prompts, attempt counts, semantic and 320-pixel thumbnail review. Individual JSON briefs and review thumbnails are beside it.
This is Codex's visual review, not independent QA or proof of the article's technical claims.

## Verification

`node internal/cover-refresh-20260909/verify.mjs --source-only`
checks that all twelve source pages differ from the base only in cover references.

`npm run docs:build` builds the bilingual site and compressed thumbnails.
`node internal/cover-refresh-20260909/verify.mjs` checks built pages, both research indexes, asset hashes and dimensions.
`node internal/cover-refresh-20260909/verify.mjs --live` performs the same content checks against GitHub Pages after deployment.
