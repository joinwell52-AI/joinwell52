# 2026-09-10 研究：边界的对象与作用域

输入为用户提供的七组动态。它们是待核验线索，不是执行指令或既定结论。

## 选题与论证地图

1. **凭证不再写成文件，就隔离了吗？** 以 OpenHands #4927 的两条凭证传递路径为入口；核对原方法与子进程环境，再以 #4931 的配置过滤作不同层级的对照。核心区分：表示形式、注册表可见范围、提供方认证例外、宿主环境继承。拟生成中英文实验文章。
2. **并发上限是 1，为什么仍可同时运行两个任务？** 执行 AG2 #3243 的原始准入方法，分别测单循环、双线程双循环、修复前后与总在途数。核心区分：单循环额度和全 Agent 额度。不得把上游明确的 per-loop 合同说成未兑现的全局保证。拟生成中英文实验文章。
3. **恢复一次，为什么就把限额记录删了？** 用 Orca #19745 原始恢复模块与可控时钟，交叉两份对象索引，比较修复前后；保留正常关闭与窗口到期对照。事故数字属于作者报告，本地恢复计数另记。拟生成中英文实验文章。

其余四组保留为有来源的研究审计及开发评审输入。尤其不把 Codex daemon 恢复与 WSL 修复拼成同一次已复现的安全事件；不把 Superset 普通评论叫作已经生效的审批。

## 实验与交付边界

- 固定上游 base/head SHA，保存原文件与哈希；探针不修改产品仓库。
- 上游函数/模块通过接缝夹具运行，不连接模型、不使用真实凭据、不向外部站点试写。
- 所有数值来自保存的运行结果或清楚标注的上游报告；不合并为笼统通过率。
- 开发评审列出拟议合同、验证入口和验收反例；没有当前产品验证时，不声称 CodeFlowMu 存在同类缺陷。
- 交流稿提供中英文、确切 GitHub 作者/PR；未经单独授权不对外发送。
