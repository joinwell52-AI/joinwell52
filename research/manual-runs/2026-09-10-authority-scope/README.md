# Authority scope: publication evidence

Three bilingual articles on credential delivery, concurrency scope and recovery history. User authorization on 2026-09-10: independently review, revise and publish the complete articles and visuals. This is a manually requested release, separate from scheduled Daily Runtime stage completion.

The release includes bounded first-party observations and original probe harnesses. Upstream source is retrieved by immutable URL with SHA-256 verification, and is not redistributed here. Run `node fetch-pinned.mjs`, then the probes using Python 3.10 and Node 24.16.0. Saved rounds 1 and 2 are historical observations; new rounds must use a different number.

See [methods](02-experiments.md), [source identities](sources/index.json), [source hashes](sources/code-manifest.json) and [review](publication-review.json). Conceptual covers are not experiment screenshots. Development review remains a separate unapproved implementation proposal.
