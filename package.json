{
  "name": "joinwell52-research-center-portal",
  "private": true,
  "version": "6.0.0",
  "type": "module",
  "scripts": {
    "runtime:validate": "node scripts/runtime-center.mjs validate",
    "runtime:build": "node scripts/runtime-center.mjs build",
    "runtime:gate": "node scripts/runtime-center.mjs gate",
    "docs:dev": "npm run runtime:build && vitepress dev docs",
    "docs:build": "npm run runtime:build && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.13"
  }
}
