{
  "name": "joinwell52-research-center-portal",
  "private": true,
  "version": "6.0.0",
  "type": "module",
  "scripts": {
    "runtime:validate": "node scripts/runtime-center.mjs validate && node scripts/runtime-results.mjs validate",
    "runtime:build": "node scripts/runtime-center.mjs build && node scripts/runtime-results.mjs build",
    "runtime:gate": "node scripts/runtime-center.mjs gate",
    "tmpa:s0.4:conformance": "node research/conformance/tmpa-core-s0.4/runner.mjs",
    "docs:dev": "npm run runtime:build && vitepress dev docs",
    "docs:build": "npm run runtime:build && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "vitepress": "^1.6.4",
    "vue": "^3.5.13"
  }
}
