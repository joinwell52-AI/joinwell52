import{_ as a,C as n,o as s,c as i,E as o,a2 as r}from"./chunks/framework.Cm6XCzck.js";const y=JSON.parse('{"title":"Weekly 007 — Recovery Is Re-Admission","description":"","frontmatter":{"title":"Weekly 007 — Recovery Is Re-Admission","date":"2026-08-30","column":"digital-employee","category":"weekly","article_type":"research-brief","edition":"research-center","research_question":"When an agent runtime resumes from durable state, what must be re-established before continuity is allowed to become execution again?","summary":"Twenty-one evidence-validated Daily Research notes converge on one boundary: recovery is not restoration. Checkpoints, cached policy, reconstructed context, trusted paths and scheduler state can preserve useful evidence, but execution must be re-admitted against current authority, ownership, occurrence identity, lifecycle closure and replay integrity.","sources":["2026-08-24 through 2026-08-30 Daily Runtime V5 publications","research/intelligence/p2-runs/2026/08/2026-08-30-p2-special.json"],"evidence_status":"Completed","citation_status":"Completed","editing_status":"Completed","publication_authorized":true,"cover":"/assets/covers/weekly-007-recovery-is-readmission-cover-v2.png"},"headers":[],"relativePath":"en/research/weekly/weekly-007.md","filePath":"en/research/weekly/weekly-007.md","lastUpdated":1788162175000}'),c={name:"en/research/weekly/weekly-007.md"};function l(d,e,h,u,p,m){const t=n("ArticleCover");return s(),i("div",null,[o(t,{image:"/assets/covers/weekly-007-recovery-is-readmission-cover-v2.png",kicker:"Weekly Research · 007",title:"Recovery Is Re-Admission",summary:"Durable state can reconstruct continuity. It cannot grant current execution authority by itself.",version:"W007",status:"Published 2026-08-30",languageHref:"/zh/research/weekly/weekly-007",languageLabel:"简体中文"}),e[0]||(e[0]=r(`<h1 id="weekly-007-—-recovery-is-re-admission" tabindex="-1">Weekly 007 — Recovery Is Re-Admission <a class="header-anchor" href="#weekly-007-—-recovery-is-re-admission" aria-label="Permalink to &quot;Weekly 007 — Recovery Is Re-Admission&quot;">​</a></h1><p>A checkpoint survives. A session can be reconstructed. A cached policy still parses. A scheduler says a worker is running. A trusted path proves where a skill came from. A prior approval remains locally available.</p><p>Every one of those facts can be useful during recovery. None of them, by itself, answers the question that matters most:</p><blockquote><p><strong>May this execution continue now?</strong></p></blockquote><p>Across 21 evidence-validated Daily Research notes published from August 24 through August 30, the same distinction appears in different mechanisms. State continuity and execution authority are separate control planes. A runtime that collapses them can recover the right bytes, the right identifiers and even the right history while still resuming the wrong work, under stale permissions, against the wrong occurrence, with incomplete ownership cleanup or with a checkpoint whose replay chain is no longer valid.</p><p>Last week’s synthesis argued that authority needs lineage. This week adds a stricter consequence:</p><blockquote><p><strong>Lineage can make state explainable, but recovery still needs a fresh admission decision.</strong></p></blockquote><p>The weekly judgment is therefore:</p><blockquote><p><strong>Recovery should be modeled as re-admission. Durable state supplies evidence for reconstruction; current authority, ownership, occurrence identity, lifecycle closure, budget scope and replay integrity determine whether reconstructed state is allowed to become executable again.</strong></p></blockquote><h2 id="evidence-scope" tabindex="-1">Evidence scope <a class="header-anchor" href="#evidence-scope" aria-label="Permalink to &quot;Evidence scope&quot;">​</a></h2><p>This synthesis uses only the seven Daily Runtime publication days from <strong>2026-08-24 through 2026-08-30</strong>, each with <code>publication = Completed</code>. The window contains <strong>21 published Research Center notes</strong>, three per day across Digital Employee, Industry Architecture and Open-source Engineering.</p><table tabindex="0"><thead><tr><th>Date</th><th>Digital Employee</th><th>Industry Architecture</th><th>Open-source Engineering</th></tr></thead><tbody><tr><td>Aug 24</td><td>Repeated failure is not new evidence</td><td>Instruction lineage is not authority</td><td>Cancellation ends waiting, not ownership</td></tr><tr><td>Aug 25</td><td>Precedence is not configuration authority</td><td>A checkpoint is not permission to resume</td><td>Finding a resource is not owning it</td></tr><tr><td>Aug 26</td><td>Foreground completion is not workflow completion</td><td>Permission authority belongs to the attachment</td><td>Copy the options, keep the client</td></tr><tr><td>Aug 27</td><td>Running is an evidence claim</td><td>Authority context must be host-minted</td><td>A trusted path proves provenance, not approval</td></tr><tr><td>Aug 28</td><td>Delegation budgets belong to the root objective</td><td>Cached policy is evidence, not current authority</td><td>Approval caches need an authorization identity</td></tr><tr><td>Aug 29</td><td>Restoring context does not restore authority</td><td>Trust must change the executable surface</td><td>Timeouts must close the lifecycle they own</td></tr><tr><td>Aug 30</td><td>A resume needs more than a checkpoint</td><td>Stronger-looking evidence does not make action safe</td><td>A durable checkpoint can still be unrecoverable</td></tr></tbody></table><p>The P2 lane separately checked all four due <code>biweekly-or-release</code> objects. All four resolved with no material post-checkpoint mechanism change; two monthly objects were not due because their August baseline already completed; no trigger reached 5; no full P2 Special Study ran. That lane therefore contributes a negative but useful fact: the week’s synthesis is driven by the validated Daily evidence, not by a newly triggered P2 study.</p><h2 id="the-central-mistake-treating-reconstruction-as-authorization" tabindex="-1">The central mistake: treating reconstruction as authorization <a class="header-anchor" href="#the-central-mistake-treating-reconstruction-as-authorization" aria-label="Permalink to &quot;The central mistake: treating reconstruction as authorization&quot;">​</a></h2><p>Recovery systems often begin with a practical goal: avoid losing work. From that goal it is easy to build a chain of seemingly reasonable equivalences:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>state exists</span></span>
<span class="line"><span>→ state is recent</span></span>
<span class="line"><span>→ state is reconstructable</span></span>
<span class="line"><span>→ state is safe to resume</span></span>
<span class="line"><span>→ execution may continue</span></span></code></pre></div><p>The week’s evidence breaks this chain in several places.</p><p>A durable Session append can have an uncertain commit outcome, so a checkpoint is not sufficient until authoritative history is reconciled. A reconstructed agent context can be complete enough to restore conversation continuity while still lacking current permission to execute. A DeltaChannel checkpoint can be durably stored while its seed snapshot, ordered writes, reducer identity or migration assumptions are no longer replay-valid. A cached policy can be readable while the system explicitly requires fresh remote authority. A previously trusted workspace can retain state while current trust should narrow the capability surface.</p><p>These are not variations of “persistence failed.” Persistence often succeeded. The failure is stronger: <strong>persistence answered the wrong question</strong>.</p><p>A recovery boundary needs at least two proofs:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Reconstruction proof: Can the prior state be rebuilt faithfully?</span></span>
<span class="line"><span>Admission proof: May that rebuilt state execute under current conditions?</span></span></code></pre></div><p>The first is about continuity. The second is about authority.</p><h2 id="checkpoints-are-candidate-evidence-not-continuation-tokens" tabindex="-1">Checkpoints are candidate evidence, not continuation tokens <a class="header-anchor" href="#checkpoints-are-candidate-evidence-not-continuation-tokens" aria-label="Permalink to &quot;Checkpoints are candidate evidence, not continuation tokens&quot;">​</a></h2><p>Three notes sharpen this distinction directly.</p><h3 id="uncertain-persistence-must-be-reconciled-before-resume" tabindex="-1">Uncertain persistence must be reconciled before resume <a class="header-anchor" href="#uncertain-persistence-must-be-reconciled-before-resume" aria-label="Permalink to &quot;Uncertain persistence must be reconciled before resume&quot;">​</a></h3><p>The Aug 25 Industry Architecture note studies a Session append whose durable commit status is unknown after interruption. The safe response is not blind replay and not optimistic continuation. The uncertainty itself becomes durable recovery state, and model execution waits until authoritative history is reconciled.</p><p>This creates a useful invariant:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unknown commit outcome</span></span>
<span class="line"><span>≠ safe retry</span></span>
<span class="line"><span>≠ safe continue</span></span></code></pre></div><p>The runtime first resolves what actually became durable, then decides what to do next.</p><h3 id="restored-context-is-not-restored-authority" tabindex="-1">Restored context is not restored authority <a class="header-anchor" href="#restored-context-is-not-restored-authority" aria-label="Permalink to &quot;Restored context is not restored authority&quot;">​</a></h3><p>The Aug 29 Digital Employee note makes the separation explicit. A full state snapshot can be eligible for context reconstruction, but reconstruction evidence does not become current execution permission.</p><p>A resumed worker may know exactly what it was doing and still be unauthorized to keep doing it because the policy epoch changed, the principal changed, the workspace lost trust, a grant was revoked, a budget was exhausted, or the original occurrence is no longer live.</p><p>This suggests a practical rule:</p><blockquote><p><strong>Restore descriptive state before restoring executable state.</strong></p></blockquote><p>The runtime may rebuild history, memory and local variables first. Tools, privileged capabilities and pending side effects should materialize only after a fresh admission decision.</p><h3 id="durable-does-not-mean-replayable" tabindex="-1">Durable does not mean replayable <a class="header-anchor" href="#durable-does-not-mean-replayable" aria-label="Permalink to &quot;Durable does not mean replayable&quot;">​</a></h3><p>The Aug 30 engineering note adds a deeper checkpoint problem. Delta-style persistence can turn a checkpoint into a reference graph: a seed snapshot plus an ordered write chain interpreted by a reducer. Every blob can be durable and the checkpoint can still be unrecoverable if one referenced element disappears or if replay semantics drift.</p><p>Recovery therefore needs a replay-integrity proof, not merely storage existence:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>seed reachable</span></span>
<span class="line"><span>+ write chain complete and ordered</span></span>
<span class="line"><span>+ reducer identity compatible</span></span>
<span class="line"><span>+ migration valid</span></span>
<span class="line"><span>= reconstructable checkpoint</span></span></code></pre></div><p>Only after that proof does current execution admission even become relevant.</p><h2 id="current-authority-must-dominate-stale-but-useful-evidence" tabindex="-1">Current authority must dominate stale but useful evidence <a class="header-anchor" href="#current-authority-must-dominate-stale-but-useful-evidence" aria-label="Permalink to &quot;Current authority must dominate stale but useful evidence&quot;">​</a></h2><p>The second cluster of Daily evidence concerns authority freshness.</p><h3 id="configuration-precedence-cannot-override-security-ownership" tabindex="-1">Configuration precedence cannot override security ownership <a class="header-anchor" href="#configuration-precedence-cannot-override-security-ownership" aria-label="Permalink to &quot;Configuration precedence cannot override security ownership&quot;">​</a></h3><p>The Aug 25 Digital Employee note shows a security-sensitive broker becoming authoritative for credential-provider state. Ordinary configuration precedence is not enough because a lower-trust project layer can otherwise regain control merely by winning a merge rule.</p><p>The safe architecture removes protected inputs from the lower-trust layer rather than giving them a lower priority.</p><p>This is a recovery lesson too. When state is reconstructed from layered configuration, the runtime must ask not only which value wins, but <strong>which layer is allowed to supply this class of value at all</strong>.</p><h3 id="cached-policy-can-remain-evidence-without-remaining-authority" tabindex="-1">Cached policy can remain evidence without remaining authority <a class="header-anchor" href="#cached-policy-can-remain-evidence-without-remaining-authority" aria-label="Permalink to &quot;Cached policy can remain evidence without remaining authority&quot;">​</a></h3><p>The Aug 28 Industry Architecture note makes a precise distinction: stale policy may still be readable and diagnostically useful while being inadmissible for governed capability decisions when fresh remote policy is required.</p><p>This avoids a common availability shortcut:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fresh authority unavailable</span></span>
<span class="line"><span>→ use last known policy</span></span>
<span class="line"><span>→ silently preserve capability</span></span></code></pre></div><p>A safer system can preserve the cache as evidence while contracting the executable surface until current authority is resolved.</p><h3 id="trust-should-change-what-can-execute" tabindex="-1">Trust should change what can execute <a class="header-anchor" href="#trust-should-change-what-can-execute" aria-label="Permalink to &quot;Trust should change what can execute&quot;">​</a></h3><p>The Aug 29 Industry Architecture note shows workspace trust becoming an admission filter rather than a UI label. Restrictive signals win, unresolved trust becomes false, malformed trust configuration fails, and effective configuration is narrowed before capabilities materialize.</p><p>This provides a general recovery pattern:</p><blockquote><p><strong>Re-admission should happen before capability materialization, not after a recovered worker has already received the old tool surface.</strong></p></blockquote><h3 id="authority-evidence-should-be-minted-by-the-authority-owner" tabindex="-1">Authority evidence should be minted by the authority owner <a class="header-anchor" href="#authority-evidence-should-be-minted-by-the-authority-owner" aria-label="Permalink to &quot;Authority evidence should be minted by the authority owner&quot;">​</a></h3><p>The Aug 27 Industry Architecture note adds provenance. Caller-supplied copies of access context are removed; the host verifies current account access and injects authority context only when the required predicates hold.</p><p>This matters during recovery because state often contains serialized representations of prior grants. Replaying those representations must not recreate the grant. Current authority evidence should be re-issued by the component that owns the authority decision.</p><h2 id="recovery-also-needs-occurrence-and-owner-identity" tabindex="-1">Recovery also needs occurrence and owner identity <a class="header-anchor" href="#recovery-also-needs-occurrence-and-owner-identity" aria-label="Permalink to &quot;Recovery also needs occurrence and owner identity&quot;">​</a></h2><p>Even with current policy, a runtime can resume the wrong instance of the right work.</p><h3 id="nested-hitl-needs-compound-continuation-identity" tabindex="-1">Nested HITL needs compound continuation identity <a class="header-anchor" href="#nested-hitl-needs-compound-continuation-identity" aria-label="Permalink to &quot;Nested HITL needs compound continuation identity&quot;">​</a></h3><p>The Aug 30 Digital Employee note shows that a restored human-in-the-loop response must bind to more than a checkpoint. The runtime needs the workflow frame, the specific call occurrence and the branch before deciding whether to continue, pause or replay.</p><p>This prevents a response from being valid merely because it looks semantically appropriate to a nearby resumed state.</p><p>A useful continuation key resembles:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>workflow_frame</span></span>
<span class="line"><span>+ occurrence_id</span></span>
<span class="line"><span>+ branch_identity</span></span>
<span class="line"><span>+ current_authorization</span></span></code></pre></div><p>The exact fields vary by runtime, but the principle is stable: <strong>continuation authority belongs to a specific live occurrence, not to a generic recovered prompt or task label.</strong></p><h3 id="permission-authority-belongs-to-the-attachment" tabindex="-1">Permission authority belongs to the attachment <a class="header-anchor" href="#permission-authority-belongs-to-the-attachment" aria-label="Permalink to &quot;Permission authority belongs to the attachment&quot;">​</a></h3><p>The Aug 26 Industry Architecture note binds MCP permission profiles to enabled server attachments. This reduces ambiguity about which context owns connected-tool authority and narrows refresh scope.</p><p>During recovery, this means permission state should be rebuilt from current attachments rather than copied from an old global cache. The attachment identity is part of the authority boundary.</p><h3 id="delegation-budgets-belong-to-the-root-objective" tabindex="-1">Delegation budgets belong to the root objective <a class="header-anchor" href="#delegation-budgets-belong-to-the-root-objective" aria-label="Permalink to &quot;Delegation budgets belong to the root objective&quot;">​</a></h3><p>The Aug 28 Digital Employee note brings resource accounting into the same model. Descendants remain the source of measured usage, but the root objective that authorized delegation owns the inherited budget charge.</p><p>If a recovered child worker receives a fresh local budget without the root objective’s consumed usage, recovery silently expands authority through accounting reset.</p><p>So resource state must also survive or be re-admitted at the correct ownership level.</p><h2 id="lifecycle-closure-is-a-prerequisite-for-safe-recovery" tabindex="-1">Lifecycle closure is a prerequisite for safe recovery <a class="header-anchor" href="#lifecycle-closure-is-a-prerequisite-for-safe-recovery" aria-label="Permalink to &quot;Lifecycle closure is a prerequisite for safe recovery&quot;">​</a></h2><p>A third cluster shows that recovery cannot be correct when the previous execution’s lifecycle remains ambiguous.</p><h3 id="cancellation-ends-waiting-not-ownership" tabindex="-1">Cancellation ends waiting, not ownership <a class="header-anchor" href="#cancellation-ends-waiting-not-ownership" aria-label="Permalink to &quot;Cancellation ends waiting, not ownership&quot;">​</a></h3><p>The Aug 24 engineering note distinguishes the caller’s cancellation signal from the owner’s cleanup obligations. Cancellation during teardown is deferred long enough for bounded local cleanup, then re-raised.</p><p>This gives recovery a clean handoff condition: before a replacement worker assumes ownership, the prior owner’s local resources should be either durably transferred or explicitly closed.</p><h3 id="foreground-completion-is-not-workflow-completion" tabindex="-1">Foreground completion is not workflow completion <a class="header-anchor" href="#foreground-completion-is-not-workflow-completion" aria-label="Permalink to &quot;Foreground completion is not workflow completion&quot;">​</a></h3><p>The Aug 26 Digital Employee note extends terminal truth to tracked detached work. Parent success waits for owned in-flight outcomes instead of treating foreground return as full workflow completion.</p><p>A recovery controller therefore cannot decide “the old worker is gone, so the work is over.” It must know what work the old worker still owned and whether that ownership reached a terminal state.</p><h3 id="timeouts-must-close-the-lifecycle-they-own" tabindex="-1">Timeouts must close the lifecycle they own <a class="header-anchor" href="#timeouts-must-close-the-lifecycle-they-own" aria-label="Permalink to &quot;Timeouts must close the lifecycle they own&quot;">​</a></h3><p>The Aug 29 engineering note applies the same rule to process groups. Timeout and cancellation share bounded cleanup across the owned process group rather than terminating only the direct PID.</p><p>This is important for Agent runtimes with shells, browsers, MCP servers or subprocess tools: <strong>recovery without owner-scoped cleanup can create duplicate side effects from surviving descendants.</strong></p><h3 id="running-is-an-evidence-claim" tabindex="-1">Running is an evidence claim <a class="header-anchor" href="#running-is-an-evidence-claim" aria-label="Permalink to &quot;Running is an evidence claim&quot;">​</a></h3><p>The Aug 27 Digital Employee note ties the cluster together. A scheduler event should not be enough to publish <code>Running</code>. Scheduling, claim, process startup and readiness are different facts.</p><p>The same logic applies after recovery: a recovery slot or reconstructed checkpoint should not produce <code>Running</code> until there is fresh evidence that a worker has actually claimed the occurrence and reached the runtime’s declared readiness boundary.</p><h2 id="repeated-trusted-and-polished-evidence-can-still-be-the-wrong-evidence" tabindex="-1">Repeated, trusted and polished evidence can still be the wrong evidence <a class="header-anchor" href="#repeated-trusted-and-polished-evidence-can-still-be-the-wrong-evidence" aria-label="Permalink to &quot;Repeated, trusted and polished evidence can still be the wrong evidence&quot;">​</a></h2><p>Two notes prevent the recovery model from turning into “collect more evidence.”</p><p>The Aug 24 Digital Employee note shows a generic terminal condition being regenerated late and overwriting a more specific safety failure. Repetition is not freshness. An older, weaker condition does not become more authoritative because it is observed again during cleanup.</p><p>The Aug 27 engineering note shows that a trusted path can prove invocation provenance without proving that the invoked content is safe or currently approved. Provenance and authorization remain separate evidence identities.</p><p>The Aug 30 Industry Architecture note goes further: in a preprint covering twelve frontier models, professional evidence displays increased action commitment even on aleatorically unknowable questions, while fabricated and real panels had similar reported effects and stated belief barely moved.</p><p>The implication for recovery is important:</p><blockquote><p><strong>A recovery gate should qualify action from inspectable control evidence, not from how complete, recent, trusted-looking or persuasive the recovered state appears.</strong></p></blockquote><p>Evidence quality matters, but only when the evidence supports the proposition being decided.</p><h2 id="a-reusable-control-model-the-recovery-admission-envelope" tabindex="-1">A reusable control model: the Recovery Admission Envelope <a class="header-anchor" href="#a-reusable-control-model-the-recovery-admission-envelope" aria-label="Permalink to &quot;A reusable control model: the Recovery Admission Envelope&quot;">​</a></h2><p>The week supports a compact model for governed continuation.</p><p>A recovered execution can be represented as a candidate envelope:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RecoveryCandidate = {</span></span>
<span class="line"><span>  reconstructable_state,</span></span>
<span class="line"><span>  state_lineage,</span></span>
<span class="line"><span>  current_authority,</span></span>
<span class="line"><span>  owner_identity,</span></span>
<span class="line"><span>  occurrence_identity,</span></span>
<span class="line"><span>  lifecycle_closure,</span></span>
<span class="line"><span>  budget_scope,</span></span>
<span class="line"><span>  replay_integrity,</span></span>
<span class="line"><span>  evidence_freshness</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>The admission decision is then:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>reconstruct</span></span>
<span class="line"><span>→ verify replay integrity</span></span>
<span class="line"><span>→ resolve prior ownership/lifecycle</span></span>
<span class="line"><span>→ refresh current authority</span></span>
<span class="line"><span>→ bind owner + occurrence + branch</span></span>
<span class="line"><span>→ restore budget and capability scope</span></span>
<span class="line"><span>→ materialize executable surface</span></span>
<span class="line"><span>→ claim fresh execution</span></span>
<span class="line"><span>→ Running</span></span></code></pre></div><p>The dangerous alternative is shorter:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>checkpoint exists</span></span>
<span class="line"><span>→ load</span></span>
<span class="line"><span>→ Running</span></span></code></pre></div><p>The difference between those two flows is the difference between <strong>continuity</strong> and <strong>governed continuity</strong>.</p><p>Not every runtime needs every field literally. A low-risk read-only assistant may use a lighter envelope. A system that can write code, send messages, mutate enterprise records, invoke remote tools or maintain long-running digital-employee state needs stronger evidence because recovery can duplicate or revive consequential authority.</p><h2 id="what-changed-relative-to-last-week" tabindex="-1">What changed relative to last week <a class="header-anchor" href="#what-changed-relative-to-last-week" aria-label="Permalink to &quot;What changed relative to last week&quot;">​</a></h2><p>Weekly 006 proposed provenance-preserving admission across state transformations. Weekly 007 asks what happens when the transformation is discontinuous: crash, timeout, cancellation, checkpoint restore, policy refresh, worker replacement or human-resume.</p><p>The relationship is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lineage tells us what survived</span></span>
<span class="line"><span>reconstruction tells us what can be rebuilt</span></span>
<span class="line"><span>re-admission tells us what may execute now</span></span></code></pre></div><p>These are complementary, not interchangeable.</p><p>A runtime can have excellent lineage and fail re-admission by restoring a revoked tool grant. It can have current permission and fail reconstruction because a delta checkpoint lost its seed. It can have both and still duplicate work because the old process group or detached task was never closed. It can close the lifecycle correctly and still attach a human answer to the wrong occurrence.</p><p>Reliable recovery therefore needs a conjunction of proofs rather than one universal “resume token.”</p><h2 id="tensions-the-model-does-not-eliminate" tabindex="-1">Tensions the model does not eliminate <a class="header-anchor" href="#tensions-the-model-does-not-eliminate" aria-label="Permalink to &quot;Tensions the model does not eliminate&quot;">​</a></h2><h3 id="fresh-authority-versus-availability" tabindex="-1">Fresh authority versus availability <a class="header-anchor" href="#fresh-authority-versus-availability" aria-label="Permalink to &quot;Fresh authority versus availability&quot;">​</a></h3><p>Current policy may be temporarily unreachable. High-assurance operations should often contract capabilities, but fully fail-closed behavior can make low-risk work unnecessarily unavailable. Systems need explicit rules for which operations may use bounded stale evidence and which require fresh authority.</p><h3 id="strong-occurrence-identity-versus-migration" tabindex="-1">Strong occurrence identity versus migration <a class="header-anchor" href="#strong-occurrence-identity-versus-migration" aria-label="Permalink to &quot;Strong occurrence identity versus migration&quot;">​</a></h3><p>Strict binding makes replay safer but can complicate legitimate workflow migration. A restarted orchestrator may need to map old occurrence identities into a new execution epoch. That mapping itself should be an auditable authority transition, not an implicit string rewrite.</p><h3 id="cleanup-completeness-versus-bounded-recovery-time" tabindex="-1">Cleanup completeness versus bounded recovery time <a class="header-anchor" href="#cleanup-completeness-versus-bounded-recovery-time" aria-label="Permalink to &quot;Cleanup completeness versus bounded recovery time&quot;">​</a></h3><p>Waiting forever for perfect remote cleanup defeats recovery. The practical target is bounded ownership closure: record what was locally closed, what remains uncertain, and which external effects require reconciliation before replay.</p><h3 id="replay-integrity-versus-storage-cost" tabindex="-1">Replay integrity versus storage cost <a class="header-anchor" href="#replay-integrity-versus-storage-cost" aria-label="Permalink to &quot;Replay integrity versus storage cost&quot;">​</a></h3><p>Self-contained snapshots simplify recovery but cost more storage; delta chains are efficient but introduce dependency and migration risk. The right compaction strategy depends on how expensive an unrecoverable checkpoint is relative to storage and verification cost.</p><h3 id="root-accounting-versus-elastic-delegation" tabindex="-1">Root accounting versus elastic delegation <a class="header-anchor" href="#root-accounting-versus-elastic-delegation" aria-label="Permalink to &quot;Root accounting versus elastic delegation&quot;">​</a></h3><p>Shared budgets prevent descendants from escaping limits, but some systems legitimately reallocate resources during recovery. Such expansion should be a fresh grant owned by the root objective, not an accidental reset caused by worker replacement.</p><h2 id="predictions" tabindex="-1">Predictions <a class="header-anchor" href="#predictions" aria-label="Permalink to &quot;Predictions&quot;">​</a></h2><p>These are Research Center interpretations of recurring evidence, not claims made by any one source.</p><ol><li><strong>Resume protocols will expose explicit re-admission states.</strong> <code>Restored</code>, <code>Reconciled</code>, <code>Admitted</code>, <code>Claimed</code> and <code>Running</code> will increasingly be separate states.</li><li><strong>Checkpoint formats will carry replay dependencies.</strong> Seed identity, reducer/version identity, ordered-write completeness and migration status will become inspectable recovery metadata.</li><li><strong>Policy caches will become non-authorizing by default for high-risk capabilities.</strong> They will remain diagnostic evidence but will not silently preserve privileged execution.</li><li><strong>Worker leases will become evidence-bearing.</strong> A stale <code>Running</code> record will need heartbeat, claim freshness or checkpoint progress rather than only a long maximum-runtime window.</li><li><strong>HITL continuation will become occurrence-bound.</strong> Human responses will bind to workflow frame and call occurrence, with authorization checked separately.</li><li><strong>Delegated resource budgets will survive worker replacement at the objective level.</strong> Recovery will not reset descendant usage merely because execution moved to a new worker.</li><li><strong>Lifecycle recovery will reconcile owned descendants before replay.</strong> Process groups, detached tasks and remote side effects will increasingly appear in recovery records.</li></ol><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ul><li>What is the minimum Recovery Admission Envelope that can be standardized across Agent runtimes?</li><li>Which authority facts must always be refreshed, and which may use bounded stale evidence?</li><li>How should a runtime prove that an old execution epoch is sufficiently closed before granting a new one?</li><li>Can checkpoint replay integrity be verified incrementally without reconstructing the full state on every recovery?</li><li>How should human approvals migrate when a workflow occurrence is intentionally replaced?</li><li>What evidence should downgrade a stale <code>Running</code> state to recoverable <code>Waiting</code> before a maximum runtime expires?</li><li>How should remote side effects be reconciled when local ownership closes but external completion remains unknowable?</li></ul><h2 id="weekly-judgment" tabindex="-1">Weekly judgment <a class="header-anchor" href="#weekly-judgment" aria-label="Permalink to &quot;Weekly judgment&quot;">​</a></h2><p>The week does not support the simple slogan that better persistence makes agents reliable. Persistence is necessary, but it preserves only what was written. Reliability requires deciding whether preserved state is still complete, attributable, owned, replayable and authorized.</p><p>The stronger design rule is:</p><blockquote><p><strong>Never let recovered state authorize its own continuation. Reconstruct first, then independently re-admit execution from current evidence.</strong></p></blockquote><p>That rule connects checkpoints, policy caches, workspace trust, host-minted access context, approval caches, delegated budgets, cancellation cleanup, detached work, process timeouts, HITL resume and honest <code>Running</code> state. It turns recovery from a storage feature into a governed execution boundary.</p><h2 id="evidence-map" tabindex="-1">Evidence map <a class="header-anchor" href="#evidence-map" aria-label="Permalink to &quot;Evidence map&quot;">​</a></h2><p>The synthesis is grounded in the 21 published Daily Research notes listed in the evidence table above and the completed P2 check record at <code>research/intelligence/p2-runs/2026/08/2026-08-30-p2-special.json</code>. The Daily notes retain their own primary-source citations and bounded claims; this Weekly brief synthesizes those validated findings rather than replacing their source-level evidence.</p>`,135))])}const b=a(c,[["render",l]]);export{y as __pageData,b as default};
