import{_ as o,C as t,o as s,c as a,E as i,a2 as c}from"./chunks/framework.Cm6XCzck.js";const f=JSON.parse(`{"title":"The Process Is Alive. Is It Still the Original Executor?","description":"","frontmatter":{"title":"The Process Is Alive. Is It Still the Original Executor?","date":"2026-09-03","published_date":"2026-09-04","column":"open-source-engineering","category":"daily","article_type":"experiment-report","edition":"research-center","research_question":"Does an old execution record reliably identify the currently live process as its original owner?","summary":"Codex's Windows process-identity changes highlight a distinction: a live PID is not necessarily the original executor. In a controlled CodeFlowMu comparison, writer locks recognized a timestamp contradiction while approval records retained executing.","cover":"/assets/host-research-20260903/cover-02-process-identity-v1.png","language":"en","lifecycle":"Published","publication_authorized":true,"evidence_status":"Controlled durable-state reproduction on V2.2.6; no induced OS PID reuse or external execution"},"headers":[],"relativePath":"en/engineering/2026-09-03-process-alive-owner-identity.md","filePath":"en/engineering/2026-09-03-process-alive-owner-identity.md","lastUpdated":1788535641000}`),d={name:"en/engineering/2026-09-03-process-alive-owner-identity.md"};function l(p,e,h,u,g,m){const r=t("ArticleCover"),n=t("ArticleTableScroll");return s(),a("div",null,[i(r,{image:"/assets/host-research-20260903/cover-02-process-identity-v1.png",kicker:"Open-source engineering · Controlled experiment",title:"The Process Is Alive. Is It Still the Original Executor?",summary:"A live PID does not establish executor identity. Two existing record paths interpret the same controlled timestamp contradiction differently.",version:"2026-09-03",languageHref:"/zh/engineering/2026-09-03-process-alive-owner-identity",languageLabel:"简体中文"}),i(n,{language:"en"}),e[0]||(e[0]=c(`<h1 id="the-process-is-alive-is-it-still-the-original-executor" tabindex="-1">The Process Is Alive. Is It Still the Original Executor? <a class="header-anchor" href="#the-process-is-alive-is-it-still-the-original-executor" aria-label="Permalink to &quot;The Process Is Alive. Is It Still the Original Executor?&quot;">​</a></h1><p>An Agent execution process exits. Later, the operating system assigns the same process number to another process.</p><p>When the old execution record is read again, the system checks the number. It is alive.</p><p>If recovery logic stops there, the record may continue to say “executing.” What it found was not the previous executor, but a process that happens to use that number now.</p><p>This is a scenario to guard against, not a production incident we claim occurred. What drew our attention was a change to managed Windows process identity in OpenAI Codex. Following that external change into CodeFlowMu revealed something more specific than a complete lack of protection: <strong>the protection exists in one place, but the same semantics do not reach another execution record</strong>.</p><h2 id="_1-why-does-codex-preserve-more-than-a-pid" tabindex="-1">1. Why does Codex preserve more than a PID? <a class="header-anchor" href="#_1-why-does-codex-preserve-more-than-a-pid" aria-label="Permalink to &quot;1. Why does Codex preserve more than a PID?&quot;">​</a></h2><p>Codex is OpenAI&#39;s coding Agent, and its app-server provides a runtime interface for clients. When such a service runs in the background, starting, stopping and upgrading it require knowing which process is being operated on.</p><p>In <a href="https://github.com/openai/codex/pull/42381" target="_blank" rel="noreferrer">Windows lifecycle PR #42381</a>, process management uses a Windows process handle and creation time in addition to a PID. It rechecks identity before termination, avoiding confusion with a later process that reused the number. The subsequently merged <a href="https://github.com/openai/codex/pull/42392" target="_blank" rel="noreferrer">upgrade handoff PR #42392</a> also addresses successor readiness and ownership handoff. These are merged source changes, not a claim that we tested every released form of them.</p><p>The important relation is not exclusively a Windows trick: a PID is a locator, not a permanent identity. Once a long-lived Agent system writes a number to disk, its next read crosses time. The same number does not establish continuity of responsibility.</p><p>Learning from an external project does not mean copying a daemon. First, we had to ask whether CodeFlowMu already addressed this problem.</p><h2 id="_2-an-answer-already-exists-locally-but-not-in-every-record" tabindex="-1">2. An answer already exists locally, but not in every record <a class="header-anchor" href="#_2-an-answer-already-exists-locally-but-not-in-every-record" aria-label="Permalink to &quot;2. An answer already exists locally, but not in every record&quot;">​</a></h2><p>CodeFlowMu is the local multi-Agent collaboration system we are developing. This source inspection and experiment were fixed at V2.2.6, commit <code>5c94d8c3b0147b779b17f620b811c6a17cc65288</code>.</p><p>Its Runtime writer lock already protects process identity. The lock prevents multiple instances from owning the same write scope; its owner is the process holding the lock.</p><p>On Windows, <code>runtime-process-identity.ts</code> queries actual process creation time and produces an identity token that retains time precision. <code>runtimeLockOwnerIsStale()</code> does more than check liveness: when a creation token exists it compares tokens. For legacy locks without that token, it checks a temporal contradiction—if the current process was born after the lock was acquired, it cannot be the original owner. Missing permission or unavailable identity does not automatically establish that the original owner is dead.</p><p>This finding ruled out an overbroad article premise: we cannot say CodeFlowMu uses only PIDs and has no process-identity protection.</p><p>Another path is the operation approval service. It stores an approved action&#39;s execution record, including <code>executing</code>, an executor PID and a start time. When reading the record, it attempts to identify an interrupted executor. The central condition is:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">record.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;executing&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">record.execution.executor_pid </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> process.pid </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isProcessAlive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(record.execution.executor_pid)</span></span></code></pre></div><p>When that condition holds, the service changes the record to <code>partial_failed</code> and asks for inspection of the target outcome. Unlike the writer lock, this recovery logic does not compare process creation identity. If the saved PID equals the current process&#39;s PID, even this death check is skipped.</p><p>The components have different responsibilities and need not produce identical states. But they share a prerequisite question: <strong>can this old record still be attributed to the process currently found under that number?</strong></p><h2 id="_3-we-did-not-wait-for-real-pid-reuse" tabindex="-1">3. We did not wait for real PID reuse <a class="header-anchor" href="#_3-we-did-not-wait-for-real-pid-reuse" aria-label="Permalink to &quot;3. We did not wait for real PID reuse&quot;">​</a></h2><p>We did not repeatedly create processes until the operating system reassigned a particular number. Nor did we interrupt a real business operation.</p><p>The experiment created isolated approval records through the actual <code>OperationApprovalService.prepare()</code> and <code>approve()</code> methods. It then controlled the saved execution state, PID and start time. A new service instance read the record through the real <code>get()</code> method, triggering its recovery judgment. The executor was never called.</p><p>For comparison, we used the real operating-system process probe and supplied the same PID and controlled timestamp to the existing writer-lock function. This constructed a <strong>legacy lock record without a process-start token</strong>, exercising the compatibility check for a process born after the record. It did not invent two different tokens merely to obtain a predetermined mismatch.</p><p>Three input types were each repeated twice in fresh isolated directories:</p><table tabindex="0"><thead><tr><th>Controlled record</th><th>Approval state after reading</th><th>Writer-lock judgment for the same owner time conditions</th></tr></thead><tbody><tr><td>Current PID, current start time</td><td>executing</td><td>Not stale</td></tr><tr><td>A PID confirmed absent by the OS</td><td>partial_failed; inspect outcome</td><td>Stale</td></tr><tr><td>Old start time, currently live current-process PID</td><td><strong>Still executing</strong></td><td><strong>Stale</strong></td></tr></tbody></table><p>The third row is decisive. The fixture assigns a start time in 2000; the actual process was created during this experiment. The writer-lock function recognizes the contradiction, while the approval read path retains <code>executing</code>.</p><p><a href="https://joinwell52-ai.github.io/joinwell52/assets/host-research-20260903/figure-02-process-identity-en.png" target="_blank" rel="noreferrer"><img src="https://joinwell52-ai.github.io/joinwell52/assets/host-research-20260903/figure-02-process-identity-en.png" alt="The same controlled PID and old timestamp produce different judgments"></a></p><p><em>Figure 1. A synthetic old record compared with actual OS creation metadata. This is not a production PID-reuse incident. Detecting an owner mismatch does not establish an absent effect or permission to retry. Click for the full-resolution image.</em></p><p>The year 2000 is an intentionally constructed test value, not a historical log. We model the visible state of an old record encountering a current same-number process; <strong>we did not induce an OS PID-reuse event</strong>. Creating another service object also does not restart the Runtime process. The two rounds test repeatability of the controlled method, not cross-process restart reliability.</p><p>The comparison itself also needs a limit. We mapped the approval start time into the synthetic old lock&#39;s acquisition time to test the same temporal contradiction. We did not establish that production contains a real approval and a real writer lock with exactly matching timestamps.</p><p>Inputs, real OS metadata and outputs are covered by E2 in the <a href="/joinwell52/en/research/evidence/2026-09-03-host-authority-conformance">evidence guide</a>. External executor calls were zero in all six observations.</p><h2 id="_4-failure-to-recognize-interruption-is-not-proof-of-duplicate-execution" tabindex="-1">4. Failure to recognize interruption is not proof of duplicate execution <a class="header-anchor" href="#_4-failure-to-recognize-interruption-is-not-proof-of-duplicate-execution" aria-label="Permalink to &quot;4. Failure to recognize interruption is not proof of duplicate execution&quot;">​</a></h2><p>The result supports a narrow conclusion: the current approval execution record&#39;s recovery judgment distinguishes process generations less strongly than the existing writer-lock judgment. Under the constructed old-record condition, it can retain <code>executing</code> despite time evidence sufficient to reject the original owner&#39;s identity.</p><p>This may affect a user&#39;s judgment about whether to keep waiting for that executor. But our experiment did not establish that the system then initiated another action, terminated an unrelated process, or accumulated any particular number of long-running production records.</p><p>The negative control in the second row matters. When the PID really is absent, the existing service recognizes interruption and explicitly asks for outcome inspection. Recovery handling exists. The gap is distinguishing a live PID from the original process identity.</p><p>The relevant existing regressions—four process-identity tests, nine writer-lock tests and fifteen operation-approval tests—all passed twice. Existing protections are not merely designs on paper, but passing tests cannot guarantee an untested handoff condition. These are subsets of the 41-test regression selection, not extra tests to add to a new “system reliability” total.</p><h2 id="_5-an-invalid-owner-still-does-not-authorize-a-retry" tabindex="-1">5. An invalid owner still does not authorize a retry <a class="header-anchor" href="#_5-an-invalid-owner-still-does-not-authorize-a-retry" aria-label="Permalink to &quot;5. An invalid owner still does not authorize a retry&quot;">​</a></h2><p>The most dangerous fix would be to detect the missing original executor and immediately hand the action to another Agent to run again.</p><p>The old process may have completed the external action but failed to finish recording it. Process identity helps establish who still owns execution responsibility. It cannot determine whether a remote commit, file change or message send occurred.</p><p>Four questions must remain separate:</p><ul><li><strong>Liveness:</strong> does this process number currently identify a process?</li><li><strong>Identity:</strong> is it the executor named by the record?</li><li><strong>Effect facts:</strong> did the original action occur?</li><li><strong>Current authority:</strong> is that action still permitted now?</li></ul><p>Answering the first two does not automatically answer the last two.</p><p>This also applies to CodeFlowMu&#39;s FCoP file coordination. A TASK file and its lifecycle location preserve task identity and business context. File persistence alone cannot establish that an interrupted action never happened. The recovery-admission contract discussed earlier can connect with this identity research, but a newer version number does not turn all proposed components into delivered capabilities.</p><p>A direction worth review is to bind long-lived execution-owner records to a verifiable process generation, distinguishing match, mismatch and unknown. Reuse the existing identity mechanism where possible instead of inventing another PID check with slightly different semantics.</p><p>For old records, the current process&#39;s creation time must not be backfilled as the original executor&#39;s identity. That would turn missing evidence into a fabricated verification. Preserve unknown when reliable metadata is absent. Once interruption is recognized, continue to inspect effects rather than convert identity failure into reexecution permission.</p><p>These remain engineering proposals after research. This study changed no product code, performed no real side-effect recovery, and constitutes neither independent QA nor development authorization.</p><p>The external Codex change was valuable precisely because it led to a more specific question: is the same identity fact interpreted consistently by every record that depends on it?</p><p><strong>A process number still in use does not mean the old responsibility still has an owner.</strong></p>`,48))])}const w=o(d,[["render",l]]);export{f as __pageData,w as default};
