# 中断接管研究公开证据包

研究日期：2026-09-01；公开整理：2026-09-02。受测基线：`919c3b48cba31e376b45e60506fa14e4bbcfcb23`。

本包公开**已保存研究记录的转录、原始研究 fixture 的可移植模板和来源映射**。这次发布没有重新运行产品。研究文件只保留了命令、测试汇总与观察表，没有完整逐行 stdout；本包不伪造原始日志，也不将作者记录冒充独立 QA。

## 文件与主张

- `observations.json`：RA-4/RA-5、RA-7/RA-8、DC-1～DC-4 的转录；RUN-004 的 57 pass / 0 fail / 0 skipped 与七个测试文件。各集合不可相加成可靠性总分。
- `sources.json`：固定基线、来源核验日期和外部 PR 范围。AG2 #3222 与 Paperclip #12616 已合入；后者仍是默认关闭的实验性设计。
- `probe-template.ts.txt`：原研究测试，仅把原机器专用 import 根改成 `__CODEFLOWMU_BASELINE__`。未改断言、合成值或测试逻辑。原 fixture 的 `quarantine` 是早期输入值；公开合同名称为 `hold_for_review`，不回写历史数据。
- `check.mjs`：检查上述转录关系与文件摘要，不调用 Runtime、不证明产品行为。
- `manifest.json`：SHA-256 清单，只检测相对此包的改动，不提供独立见证。

## 公开检查

下载整个 ZIP 并解压，在本包目录运行：

```text
node check.mjs
```

预期输出 PASS、退出码 0。这里只证明保存数据内部一致、附件未相对清单改变。

## 需要授权源码的产品复跑

须有固定基线和安装依赖。先在唯一产品目录 `D:\codeflowmu` 只读核对版本；本包不授权 checkout、覆盖本地改动或新建产品副本。版本不匹配时停止，不能把新版本结果冒充历史复跑。

由获授权操作者把模板 import 标记替换为该已核验根目录并保存为研究测试文件，使用已有 tsx 运行环境执行。测试调用真实 SessionManager/Dispatcher，SDK 为内存替身，FCoP 文件仅在测试隔离目录合成；RA-7/RA-8 的效果语义来自合成事件，退避设为零。完整 RUN-004 命令及测试文件在 `observations.json` 记录；运行属于另一次实验，需另存原始输出，不能追加成这里已经发生的事实。

## 限制

没有真实掉电、远端副作用、浏览器 E2E 或独立 QA。两场景同回 inbox 只证明该恢复入口没有区分给定的效果语义，不证明生产重复执行。57 项既有测试只支持组件职责边界，不是冻结后 IA-1～IA-12、DC-1～DC-3 的正式验收。
